# Dataset preparation configuration
# Configuration for fusion data processing pipeline

# Logging configuration
log_level: INFO

# Signal configuration - list of signals to process
# Each signal has: [signal_name, abbreviation, should_transform]
signal:
  magnetics_high_resolution:
    abbr: "mhr"
    make_stft: true
    expected_channels: 8
  ece_cali:
    abbr: "ece"
    make_stft: true
    expected_channels: 48
  co2_density:
    abbr: "co2"
    make_stft: true
    expected_channels: 4
  gas:
    abbr: "gas"
    make_stft: false
    expected_channels: 5
  ech:
    abbr: "ech"
    make_stft: false
    expected_channels: 11
  p_inj:
    abbr: "pin"
    make_stft: false
    expected_channels: 8
  t_inj:
    abbr: "tin"
    make_stft: false
    expected_channels: 8

# Data processing parameters
randomize_shots: true
random_seed: 42
num_shots: 10000
fs_khz: 500  # Sampling frequency in kHz
ip_threshold: 0.1  # Plasma current threshold
window_ms: null  # Window size in milliseconds
hop_ms: null  # Hop size in milliseconds
remove_empty: true
do_stft: true
start_time: null  # Start time for signal extraction (null for auto-detection)
end_time: null    # End time for signal extraction (null for auto-detection)

# Train/test split configuration
debug: false

# Directory paths
raw_data_dir: /scratch/gpfs/EKOLEMEN/d3d_fusion_data
output_dir: /scratch/gpfs/EKOLEMEN/hackathon/foundation25/spectrogram

# Processing parameters
stft:
  n_fft: 1024
  hop_length: 256
  
# Output settings
compression: true  # Whether to compress saved files 