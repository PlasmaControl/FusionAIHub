"""d3d_elm_time_to_event_dsm - scaffold. See README.md for what this model is and what blocks it."""

raise NotImplementedError(
    "d3d_elm_time_to_event_dsm is a scaffold: no adapter yet. "
    "Upstream weights: /projects/EKOLEMEN/wpqh_elm_hiro/hiro_scripts/. "
    "Blocked on: recover the ~124 trained-on feature names and their order from `hiro_scripts/`; decide how a deep-survival-machines head (gate, scale, shape) becomes a label series: hazard at fixed horizons, or expected time to event; the embedding network bakes in its own input normalisation, so the constants need no recovery - confirm that"
)
