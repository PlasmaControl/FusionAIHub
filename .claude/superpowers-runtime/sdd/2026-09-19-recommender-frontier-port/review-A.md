# Review — Phase A (tasks A1 / A2 / A3), a978f0e..25667aa

Read the diff in four passes with `Read`/`sed` offsets: (1) header + stat + commit list, (2) the `.gitignore` and link-rewrite block (L1997–2310), (3) the carried scripts, scanned by grep over added lines rather than line-by-line, (4) the three ignite conflict resolutions and the slurm hunks (L14400–15356). The 68 pure renames were checked from the stat summary and `git ls-tree`, not read.

### Spec Compliance
- ✅ Spec compliant, with one extra (`.gitignore`) that was genuinely mandatory and one extra commit that was justified.
- A1 — `nathan_dev` created at `origin/recommender`, all 47 untracked `nathan_fm` files carried (ruling 3), `scripts/slurm_frontier/_frontier_common.sh` + `_frontier_settings.sh` + `benchmark_plugin_perf.sh` + `train_dynamics.py` `_sync_grads` all present. Ruling 4 (prefer recommender on duplicate hunks) never triggered — verified: `git ls-tree a978f0e -- .superpowers` shows the base already carried the 39 sdd files, and the three modified files were byte-identical at 702aaae and a978f0e per the report; no recommender-side duplicate exists in the diff.
- A2 — merge `294097f` (parents `e4fe84c` + `f71acd4`). Peter's four features are all live at HEAD: `--actuator_mode` (eval_dynamics.py:1359, `divergence_vs_real` at :1162), `DynamicsConfig.dropout` (dynamics_config.py:83, distinct from `actuator_dropout_p` at :129), `_GUMBEL_SCALE` (maskgit.py:41), GT|PRED-only video panel (eval_dynamics.py:844). `load_model(ckpt_path, device) -> Tuple[MaskGITDynamics, DynamicsConfig, int]` preserved at eval_dynamics.py:121 (Task C3's contract). `MaskGITDynamics.__init__(self, cfg)` unchanged (maskgit.py:45).
- A3 — ruling 1 holds both ways: `git ls-files .superpowers` = 0, and the three live workspace dirs (`2026-09-19-recommender-frontier-port/`, `2026-08-17-ignite-rollout-quality-phase1/`, `having-latent-text-encodings-enumerated-swan/`) are present and untracked. Ruling 2 holds: both `.claude/superpowers/{specs,plans}/2026-09-19-*` files landed in `e968501`, not the follow-up. Ruling 5 holds: `git rev-parse HEAD origin/nathan_dev` both `25667aa`, `origin/recommender` absent from `git branch -r`.
- `docs/` at HEAD contains exactly the brief's 13 keep-list files and nothing else (`git ls-tree -r HEAD -- docs/` plus `ls -A docs/` — no untracked leftovers either). No superpowers spec/plan/report remains under `docs/`.
- ⚠️ Cannot verify from diff: the test results (not re-run, per instructions); byte-identity of the 47 carried files against the `nathan_fm` working tree (the source tree is gone).
- ⚠️ Plan-mandated tension, not a defect: five files the brief's keep-list retains in `docs/` are planning documents by name (`IGNITE_ROLLOUT_QUALITY_PLAN.md`, `spectrogram_tokenizer_plan.md`, `stage2_genvid_integration_plan.md`, `stage2_with_video_plan.md`, `video_tokenizer_plan.md`). They are engineering plans rather than Claude/superpowers material, and the brief names them explicitly — flagging only so the controller can confirm the global constraint was meant that narrowly.

### Strengths
- The `maskgit.py` graft is the hard part of this task and it is correct. Verified independently of the report: `exp` is monotone, so `_per_modality_reveal` (maskgit.py:382, `argsort` on raw `c`) and `_global_reveal` (maskgit.py:405, `norm_log_confidence` = `1 + log c / log V`, sampling.py:115) both recover Peter's `log c + ann·g` ordering. The `float("inf")` sentinel used to force already-revealed tokens to the front (maskgit.py:380, :406) is not collided with: the perturbed score tops out at `exp(80) ≈ 5.5e34`, finite in fp32.
- Default path really is bit-identical: the `torch.rand` draw sits inside `if _GUMBEL_SCALE > 0.0:` (maskgit.py:325–330) and `conf[m.name] = c` is the same expression as before when the branch is skipped. No extra RNG consumption at scale 0.
- Every name the grafted block reads is in scope — `dev` (maskgit.py:280), `step`/`keep_masked` (maskgit.py:285) — checked against the enclosing `generate_frame` since the hunk is cut off mid-function.
- `eval_dynamics.py` has no undefined names after the panel removal: `ax_d`, `im_d`, `cax_d`, `dmax`, `diff` return zero grep hits at HEAD, and the survivors the resolution deliberately kept (`gt_static`, `mismatched`) are still consumed at eval_dynamics.py:884–888.
- The `.gitignore` rewrite is correct and its comment explains *why* the `.claude/*` form is required. Verified by `git check-ignore`: `.claude/settings.local.json`, `.claude/settings.json`, `.claude/agents/foo.md`, `.claude/other.json` all IGNORED; `.claude/notes/`, `.claude/superpowers/`, `.claude/superpowers-runtime/` all trackable. Tracked counts match the move (8 / 23 / 39).
- Every rewritten link target exists at HEAD. Swept all `.claude/...` path references outside `.claude` against `git ls-files`: 10 resolve; the 3 that do not (`sdd/progress.md`, `sdd/task-I6b-report.md`, `sdd/task-I9a-review.md`) were never tracked at the base either (`git ls-tree a978f0e -- .superpowers` lists 39 files, none of them these), so the report's "already dangling" claim is true. The `ae_dataset.py:126` filename correction (`labeler-phase3` → `labelmaker-phase3`) resolves to a real file.
- Carried-script hygiene is clean: zero secret-shaped strings in added lines, zero `$HOME`/`/ccs/home` absolute paths, zero binary patches, cluster roots are Frontier (`/lustre/orion/fus187/proj-shared`, 87 occurrences). The single Stellar path (`inspect_spectrograms/step0_inspect.py:45`) is pre-existing and was only re-pointed at `.claude/notes`, which is the right call — left alone, `SUMMARY_PATH` would have recreated a moved file under `docs/`.
- Working tree is clean under `--untracked-files=all`, so the `.gitignore` negations did not leave newly-visible files uncommitted.

### Issues

#### Critical (Must Fix)
None.

#### Important (Should Fix)
- **The live `.superpowers/` workspace is now protected only by an unshared local exclude.** `git check-ignore -v .superpowers/sdd/.../progress.md` reports `.git/info/exclude:7`. At the base commit those 39 files were *tracked*; after A3 they are neither tracked nor covered by the committed `.gitignore` (grep for `superpowers` in `.gitignore` hits only the three `.claude/` lines, 9/16/17). `.git/info/exclude` is per-clone and never pushed, so on any fresh clone — or if this clone's exclude is reset — the live sdd workspace reappears as untracked and a `git add -A` re-commits exactly the material A3 exists to keep out of the repo. The implementer already had `.gitignore` open for the `.claude/*` rewrite; a `.superpowers/` line there is a one-line completion of the same job. Not a behavior bug and it does not invalidate the rest of the work, but it is the kind of hygiene gap that silently undoes this task later.

#### Minor (Nice to Have)
- `tests/shot_design/test_phenomena.py:692` is **91 characters**, over the 88 in `pyproject.toml:76`. The report states this line was "re-wrapped to stay inside 88 cols" and separately that "my added lines are all ≤ 88 … the one 89-char line in the diff is Peter's". Both claims are false for this line: it is the implementer's own edit and it is 3 over. (Counting all modified `.py` files, 11 added lines exceed 88; the other 10 are Peter's.) Ruff's default rule set does not select `E501`, so nothing enforces it — which is why this is Minor rather than more — but it is a verification claim that does not survive checking.
- The report's rationale for the `log → +Gumbel → exp` round-trip is misstated: it says `norm_log_confidence` "takes `log` of `c` and returns NaN on a negative input". It does not — sampling.py:115 is `conf.float().clamp_min(1e-12).log() / …`, so a negative input clamps. The round-trip is still the right construction (feeding the raw log-space score in would floor every negative score to `1e-12` and collapse the whole low-confidence tail into one tie, destroying the ranking), but the stated mechanism is wrong. The in-code comment at maskgit.py:319–322 is the defensible version; the report's is not.
- Enabling `IGNITE_MASKGIT_GUMBEL` inserts a `torch.rand` draw into the shared `generator` stream per modality per step (maskgit.py:326), so it shifts every subsequent `torch.multinomial` as well as the reveal order. The probe line `greedy vs gumbel identical? False` therefore does not isolate the reveal policy — a gumbel-on run is not a fixed-seed controlled A/B against gumbel-off. Inherent to the knob and harmless for `divergence_vs_real` (both arms of that comparison take the same path, per eval_dynamics.py:234), but the report presents it as validation that the knob "demonstrably changes the reveal order", which it does not establish on its own.
- `clamp_(max=80.0)` (maskgit.py:330) ties every score above `e^80`. Unreachable below scale ≈ 4.2, and `_per_modality_reveal` uses a non-stable `argsort` (maskgit.py:382) so ties would resolve by backend luck. Acknowledged by the implementer; worth a docstring note rather than a code change.
- `CMAP_DIFF` (eval_dynamics.py:79) is dead after the panel removal — single grep hit, its own definition.
- Two comments still describe the removed difference panel: eval_dynamics.py:573 (a function docstring listing the figure's contents for readers) and :836 (`---- video: GT | PRED | difference at mid-rollout ----`, directly above the block whose new comment explains why the difference panel is gone). Self-contradictory within four lines.
- `IGNITE_MASKGIT_GUMBEL` is documented only in the source comment; no mention in `docs/IGNITE_DESIGN.md` or any script. A rollout-quality knob nobody can find is a knob nobody uses.
- `.claude/superpowers/plans/` (1 file) beside `.claude/superpowers/plans-recommender/` (8 files) is an arbitrary split created only to dodge a move-time collision. Brief-mandated, so not a deviation; a one-command follow-up.
- Commit subject `plan: serve_llm.sbatch is Stellar-only and stays, not a Frontier deletion` is declarative, not imperative, against the global constraint's "one line, imperative, `<area>: <what>`". The other three conform.

### Assessment
**Task quality:** Approved
**Reasoning:** All three briefs' deliverables are present and every binding ruling verified independently — the maskgit graft genuinely preserves Peter's ranking under both reveal policies and leaves the default RNG stream untouched, `eval_dynamics.py` has no dangling names, the `.gitignore` form is the right one, and every rewritten link resolves. The one Important item (`.superpowers/` covered only by an unshared `.git/info/exclude`) is a one-line follow-up that does not affect any code path.
