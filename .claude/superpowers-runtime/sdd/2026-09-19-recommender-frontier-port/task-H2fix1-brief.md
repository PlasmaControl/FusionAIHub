### Task H2 fix round 1: fact-check the docs against the merged code, surface the real numbers

Worktree `/lustre/orion/fus187/scratch/nchen/FusionAIHub-wt-h`, branch `nathan_dev-h`, now rebased onto `nathan_dev` at 770ee04 (which contains Phase D simulate CLI/UI, the agy provider, blurb workers, the corpus-reader build default, the PULSE-LENGTH proxy segments 0e2985b). The H2 review (2026-09-20 01:50) found:

1–4 (Critical at the time, now a fact-check): `docs/shot-design/simulation.md`, `docs/clusters/frontier.md` (its checklist), `docs/shot-design/llm-providers.md` and `docs/reference/slurm-scripts.md` were written from task contracts before the code landed. Verify EVERY command, flag, yaml key, file path and behaviour claim in those four pages against the real files now on this branch: `src/shot_design/simulate/*.py`, `src/shot_design/ui/simulate_routes.py`, `scripts/slurm_frontier/shot_design_simulate.sh`, `scripts/slurm_frontier/shot_design_build.sh` (default `BUILD_ARGS` is `--reader corpus --no-encode`), `scripts/slurm_frontier/shot_design_encode.sh`, `scripts/slurm_frontier/_shot_design_common.sh` (login-node safe since c8843d1), `scripts/shot_design/blurb_frontier.sh`, `scripts/shot_design/demo_frontier.sh`, `src/shot_design/llm/agy.py`, `src/shot_design/llm/client.py`, `configs/shot_design/llm.yaml`, `configs/shot_design/ui.yaml`. Fix every mismatch in the docs (never in the code). Keep only what the code does.
5 (Important): two "Antigravity CLI" mentions — controller ruling: KEEP them (product name of the `agy` binary an operator must install); no change.
6 (Important): agy yaml keys/parsing details "not in the spec" — now a fact-check against `llm/agy.py` + `llm.yaml`; keep what is real, delete what is not.
7 (Minor): surface the real corpus/DB numbers everywhere the docs say 5,000 or 16.9k for Frontier: the Frontier corpus has **8,753** shot files, **3,031** eligible, the list `recommender_frontier_v1` is all 3,031 (say "target was 5,000; the Frontier corpus supplies 3,031").
8 (Minor): `src/shot_design/retrieval/phenomena.py:102` comment still says `docs/SHOT_DESIGN.md` → point it at `docs/shot-design/overview.md`.

New facts to add (short, factual, one place each — `docs/shot-design/database-build.md` and `docs/clusters/frontier.md`):
- Frontier has no plasma current in the FAITH corpus and no labeler feature store; `ip` (and the 14 other labeler-addressed features: bt betan kappa li qmin tritop tribot gapin aminor r0 volume pcbcoil ne_zipfit te_zipfit) come from GA's fdp, which works from the Frontier login node over Pelican/OSDF (HTTPS 443). Env: `.pixi/envs/fdp` (`PIXI_CACHE_DIR=/tmp/pixi-cache-nchen pixi install --frozen -e fdp`). Token: `fdp login` in the operator's shell → `~/.fdp/cache/d3d.token`, or a JWT at `~/.fdp/token` (mode 600). Command: `pixi run --frozen -e fdp fdp -D d3d run python -m labeler.run features --features ip bt betan kappa li qmin tritop tribot gapin aminor r0 volume pcbcoil ne_zipfit te_zipfit --shot-file <shots.txt> --workers 12 --root $LABELER_ROOT --corpus-dir $SHOT_DESIGN_CORPUS`; ~35 s per shot per worker; output `$LABELER_ROOT/features/<shot>_features.h5`.
- Fallback when a shot has no `ip` feature: `build` derives segments from the shot table's `PULSE-LENGTH` (ramp-up 1.0 s / ramp-down 0.27 s, sum = `select.PROXY_RAMP_S` 1.27 s) and marks the record (`coverage_reasons["ip"]`, `end_reason no_ip_signal`, no `ip` in `raw_sources`). Read `src/shot_design/shotdb/features.py::proxy_segments` and `configs/shot_design/retrieval.yaml` `segments:` for the exact keys.
- Order of the build stages on Frontier: census → select → logs (the Frontier bundles need no import; `--no-logs` is not used) → labels join → **features (fdp)** → build (`--reader corpus --no-encode`, debug QOS `-p batch -q debug -t 02:00:00` or `extended`) → encode → blurbs → coverage/describe.

Rules: docs-only changes plus the one `phenomena.py` comment. No Claude/superpowers/planning material in `docs/`. `cd website && npm run build` must pass (`onBrokenLinks: 'throw'`), and `PYTHONPATH=$PWD/src /lustre/orion/fus187/scratch/nchen/FusionAIHub/.pixi/envs/shot-design-frontier/bin/python -m pytest tests/shot_design/test_phenomena.py -q -p no:cacheprovider` must pass in the worktree (never run pixi inside the worktree). Commit in the worktree with explicit pathspecs, one line: `docs: fact-check Frontier, LLM, simulation and build pages against the merged code`. Report to `task-H2-report.md` under a new heading `## Fix round 1` with, per page, the list of claims corrected (before → after).
