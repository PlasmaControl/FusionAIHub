### Final-review fix round 1 (one dispatch): I1, I2, M1, M2, M3, M4, M9, M10, M11

Repo `/lustre/orion/fus187/scratch/nchen/FusionAIHub`, branch `nathan_dev`, HEAD 77a0678. Work directly here (no worktree). The tree has an unrelated uncommitted edit to `.claude/superpowers/plans/…` — never `git add -A`; commit with explicit pathspecs. Full review: `.claude/superpowers-runtime/sdd/2026-09-19-recommender-frontier-port/review-final.md` (read the I1/I2/M1–M4/M9–M11 entries; ignore the rest).

Python for tests: `PYTHONPATH=$PWD/src .pixi/envs/shot-design-frontier/bin/python -m pytest <files> -q -p no:cacheprovider`. Never run bare `pixi`. TDD: failing test first, then code, then green. Ruff line-length 88 on new lines. One-line imperative commits `<area>: <what>`; one commit per finding group is fine (2–4 commits total).

**I1 — controller ruling: drop `#SBATCH -q debug` from `scripts/slurm_frontier/shot_design_simulate.sh`** (the job is `-t 01:00:00` on `batch`, inside the 2 h cap without debug; Frontier's debug QOS allows ONE submitted job per user, `QOSMaxSubmitJobPerUserLimit`, which breaks the three-design demo loop and the UI's second concurrent simulation). Add a comment on the wrapper saying why, and that a one-off can still pass `sbatch -q debug …`. Update whatever `tests/shot_design/test_slurm_frontier_scripts.py` asserts about the simulate wrapper's QOS (add/adjust a test that the simulate wrapper does NOT set `-q debug`, with the reason in the test name). Update the docs that state the simulate job runs on the debug QOS: grep `docs/` for `debug` near simulate (`docs/shot-design/simulation.md`, `docs/reference/slurm-scripts.md`, `docs/clusters/frontier.md`) and correct only those sentences. `demo_frontier.sh` itself needs no change.

**I2 — `src/shot_design/simulate/cli.py` (~line 106–107):** `model, cfg, step = core.load_dynamics(paths, device)` is followed by `cfg.k0_seed, cfg.n_predict = args.k0, args.n_predict`. `cfg.max_frames` (property in `src/tokamak_foundation_model/ignite/dynamics_config.py:199`) recomputes from k0+n_predict, so the downstream guard passes and the frame embedding indexes out of range (device-side assert). Fix: `trained = cfg.max_frames` BEFORE the assignment; if `args.k0 + args.n_predict > trained`, `raise ValueError(f"--k0 + --n-predict = {total} exceeds the checkpoint's trained horizon {trained}")`. Test: in `tests/shot_design/test_simulate_cli.py` (or wherever the existing simulate CLI tests monkeypatch `core.load_dynamics`), a test where the fake cfg has max_frames 100 and `--n-predict` pushes past it → ValueError with that message, and `status.json` shows `failed` if the existing CLI wraps errors that way (mirror the existing failure test).

**M1 — `src/shot_design/design/assistant.py:~305`:** replace `client.cfg.get("provider") == "off"` with `client.off` (the `LLMClient.off` property already normalises "", None, "none", "false", casing). Test: provider `"None"`/`"false"` behaves as off.

**M2 — `src/shot_design/llm/client.py:~133 available()`:** after the agy branch, an unregistered provider returns `(False, f"unknown llm provider {provider!r}; known: <sorted names>")` instead of falling through to the ollama endpoint logic. Test beside `test_unknown_provider_raises_llmunavailable_naming_the_known_ones`.

**M3 — `src/shot_design/cli.py` `design show` parser (~line 1993):** put `--references` and `--actuation-csv` in `add_mutually_exclusive_group()`. Test: passing both exits with argparse's error (SystemExit 2).

**M4 — `src/shot_design/cli.py::_evalsets_prompt` (~704–718):** catch `(OSError, yaml.YAMLError)`; an entry mapping without `text` prints "prompt has no text" (`entry.get("text")`) and returns 1 instead of KeyError. Tests for both.

**M9 — `src/shot_design/cli.py::cmd_llm` (~682):** `(client.cfg.get(provider) or {}).get("bin", provider)` so a null provider block does not raise. Test in `tests/shot_design/test_cli_llm.py` with `cfg = {"provider": "agy", "agy": None, ...}`.

**M10 — `src/shot_design/shotdb/build.py::_bundle_pulse_length_s` (~333–352):** call `select._num` instead of the local copy (verified equivalent). Existing tests must stay green.

**M11 — `tests/shot_design/test_simulate_report.py:48`:** rename the meta literal `dynamics_sha256` → `bundle_manifest_sha256` (what `simulate/cli.py` actually writes).

Run at the end: `tests/shot_design/test_simulate_cli.py tests/shot_design/test_simulate_report.py tests/shot_design/test_llm_client.py tests/shot_design/test_cli.py tests/shot_design/test_cli_llm.py tests/shot_design/test_assistant*.py tests/shot_design/test_slurm_frontier_scripts.py tests/shot_design/test_build_proxy_segments.py tests/shot_design/test_features.py` (adjust names to what exists: `ls tests/shot_design | grep -i "simulate\|llm\|cli\|assistant\|slurm\|proxy"`), then the whole `tests/shot_design -q -p no:cacheprovider --deselect tests/shot_design/test_mcp.py` if under ~3 min (it was 128 s).

Report to `.claude/superpowers-runtime/sdd/2026-09-19-recommender-frontier-port/task-FINALfix1-report.md`: per finding, file:line, what changed, the covering test name, and the final test tail. Return only: status, commit shas, one-line test summary, concerns. Never dispatch subagents.

**I3 (found at run time, 2026-09-20 13:58, build job 5516457) — `src/shot_design/shotdb/build.py::_check_publish` (~1168):** on Frontier `shot_design labels join` ran BEFORE the first `build`, so `db/manifest.json` held only the join's `labels` block (no `shot_source`, no `n_shots`) and the first-ever build was refused with "invalid manifest fields; Use --force". A directory that has never been built (no `shots.parquet` in `db_dir`) is not a database to protect. Fix: in `_check_publish`, if `(db_dir / "shots.parquet")` does not exist, return `None` (same as the FileNotFoundError path) before validating the manifest. Test in the build tests: a db_dir holding only `manifest.json` with a `labels` block (+ the three join parquet names as empty files if the fixture needs them) lets `build` proceed without `--force`, and the join's three tables survive the publish. Commit `shot_design build: a join-only manifest does not block the first build`.
