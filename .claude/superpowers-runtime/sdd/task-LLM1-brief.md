# Task LLM1 — three-sentence per-shot summaries from the local Gemma 4 model

Worktree `/scratch/gpfs/nc1514/FusionAIHub-LLM1`, branch `recommender-LLM1` (from `recommender` @ 3a08ba0). Executor: Codex gpt-6-astra, write mode. A sibling task (UI1, worktree `FusionAIHub-UI1`) is restyling `src/shot_design/ui/**`, `retrieval/describe.py`, `schema.py`, `shotdb/store.py` at the same time — do not edit those files here beyond what is listed below; the UI reads `blurb`/`blurb_source` from `shots.parquet` and will render whatever this task writes.

## The user's request (2026-09-15)
"Having an LLM say quickly (1) the goal, (2) if it was successful, (3) any interesting findings, as the three really short sentences describing each shot" — shown in the results table and at the top of the shot page instead of the logbook quote.

## What exists (reuse, do not reinvent)
- `src/shot_design/retrieval/blurb.py`: `source_text(rec)` (MP title/purpose, run title, shot brief, ≤ 8 quotable operator entries, chief-operator status, outcome line, verdict), `_system(max_words)` prompt (v4: exactly TWO sentences), `gate()` (empty / word cap / quotation marks / invented shot numbers / invented numbers vs `describe.extract_facts`), `make(rec, client, cfg)` → `Blurb(text, source ∈ {llm, template}, reason)`, `fallback(rec)` = header + outcome line.
- `src/shot_design/shotdb/build.py::write_blurbs(paths, client, only_missing)` backfills `blurb`, `blurb_source` into `shots.parquet` atomically and updates `manifest.json["blurbs"]`; CLI `python -m shot_design blurb [--all]` (`cli.py::cmd_blurb`).
- `src/shot_design/llm/client.py::LLMClient` — OpenAI-compatible chat over httpx, endpoint discovery from `configs/shot_design/llm.yaml` (`provider`, `base_url`, `endpoint_file` = `<data_root>/llm/endpoint.json`), deterministic requests cached under `paths.llm_cache_dir`, `reasoning_effort: none` for gemma4.
- `configs/shot_design/llm.yaml`: `provider: "off"`, models `quality: gemma4:26b`, `fast: gemma4:e4b`, `blurb: {model: quality, max_words: 70, prompt_version: 4}`.
- Ollama 0.33.3 is ALREADY installed and both models are ALREADY pulled (27 GB) in the previous project's data root: binary `/scratch/gpfs/EKOLEMEN/nc1514/shot-recommender/bin/ollama/bin/ollama` (+ `lib/ollama`), models `/scratch/gpfs/EKOLEMEN/nc1514/shot-recommender/models/ollama` (`OLLAMA_MODELS`), keypair home `/scratch/gpfs/EKOLEMEN/nc1514/shot-recommender/ollama_home`. Do NOT copy or re-download them.
- The previous project's serve scripts to port: `/scratch/gpfs/nc1514/shot-recommender-system/scripts/serve_llm.sh` and `serve_llm.sbatch` (read-only reference; they use `uv run python` and `shotrec` — ours must use the mandated pixi form and `shot_design`).

## Deliverables
1. **Prompt v5** in `blurb.py`: exactly THREE plain sentences — (1) what the shot / experiment set out to do; (2) whether it succeeded, naming a disruption, fast current quench or early termination plainly when the source says so; (3) one interesting finding from the operator entries / shot brief, or, when nothing notable is recorded, the literal sentence "No notable findings were logged." Own words, no quotation marks, no invented or rounded numbers (prefer none), no headings/lists/preamble. `max_words` 90. Bump `prompt_version` to 5 and document v5 in the yaml comment block. Update `gate()` if needed so a compliant three-sentence answer passes and a two- or four-sentence answer is rejected with a reason (count sentence terminators sensibly: abbreviations like "kA." are rare in the model's own words — keep the check simple and tested). `fallback()` stays (header + outcome line), so a gated-out shot still reads sensibly.
2. **Provenance columns**: `write_blurbs` also writes `blurb_model` (tag) and `blurb_prompt_version` (int) per row; `only_missing` treats rows whose `blurb_prompt_version` < current as missing. `manifest.json["blurbs"]` gains `prompt_version` and `model`. `ShotRecord` already has `blurb`; do not touch `schema.py` beyond adding optional `blurb_source: str | None` ONLY IF it is not already there — coordinate: UI1 may add `blurb_source` to `SearchHit`; you add nothing to `SearchHit`.
3. **CLI**: `shot_design blurb` gains `--limit N` and `--dry-run` (print shot, source_text length, the model's candidate, the gate verdict and the final text; write nothing) for a quality check before the full run; `--all` keeps its meaning.
4. **Serve scripts**: `scripts/shot_design/serve_llm.sh` and `scripts/shot_design/serve_llm.sbatch`, ported from the reference: resolve `paths.data_root`, the model tags and the new `llm.yaml` keys `ollama_bin_dir`, `ollama_models_dir`, `ollama_home_dir` (defaults = the three shot-recommender paths above) with ONE `pixi run --frozen --no-install --manifest-path /scratch/gpfs/nc1514/FusionAIHub/pyproject.toml -e ideate-cpu python -c …`; write `<data_root>/llm/endpoint.json` (same JSON shape: url, models, host, job_id, started, version) atomically, remove it on exit only if it still names this server; refuse to start when the port already answers; `OLLAMA_CONTEXT_LENGTH=16384`, `OLLAMA_KEEP_ALIVE=24h`, `OLLAMA_MAX_LOADED_MODELS=2`; the pull loop stays (it is a no-op when the models are present) but must never download the binary — if `ollama_bin_dir/bin/ollama` is missing, exit 2 with the path. sbatch: `-J shot-design-llm -p gpu --gres=gpu:1 -c 8 --mem=64G -t 04:00:00 -o <data_root>/llm/slurm-%j.out`, binds `$(hostname -s):11434`, `cd $SLURM_SUBMIT_DIR`. Add a `scripts/shot_design/blurb_all.sh` wrapper that runs the mandated pixi form `python -m shot_design blurb --all` on the login node against the endpoint file (the client is CPU-side).
5. **Config**: `provider: ollama` (the client stays unavailable, hence templates, until the endpoint file exists — verify that path in a test), the three new dir keys, `blurb.max_words: 90`, `prompt_version: 5`.
6. **Tests** (write them first; fake `httpx` transport as the existing blurb/client tests do): v5 prompt content; gate accepts three sentences and rejects two/four with reasons; `write_blurbs` writes the two new columns and treats a stale prompt version as missing; `--dry-run --limit` prints and does not write (use `tmp_path` DBs from the existing fixtures); serve script: `bash -n` both scripts and a test that runs `serve_llm.sh` with a fake `ollama` shim on PATH-equivalent dir key to check the endpoint file shape and the refuse-to-double-start branch (skip cleanly if `curl` is missing). Docs: `docs/SHOT_DESIGN.md` LLM section (how to start the server, dry run, full run, what the gate does).

## Rules (binding)
- Every `pixi run` is `pixi run --frozen --no-install --manifest-path /scratch/gpfs/nc1514/FusionAIHub/pyproject.toml -e ideate-cpu|labelmaker`; never bare `pixi run`, never `pixi install/lock/update`; no `.pixi` in this worktree at the end.
- Nothing new under `/scratch/gpfs/nc1514` except this worktree's source. NO production writes: the DB at `/scratch/gpfs/EKOLEMEN/nc1514/ideate/db`, `<data_root>/llm/`, the shot-recommender root and the labeler root are read-only for you; do NOT start Ollama, do NOT submit SLURM jobs, do NOT run `blurb` against production — the controller/user does that after merge. Tests write only to `tmp_path`.
- Both suites green with `-W error` via the mandated form (tests/shot_design ≥ 1,375 passed 0 skipped; tests/labeler 1,775 passed 3 skipped), ruff clean (`/scratch/gpfs/nc1514/FusionAIHub/.pixi/envs/labelmaker/bin/ruff check --no-cache src/labeler src/shot_design scripts/labeler scripts/shot_design tests/labeler tests/shot_design`). Never commit while a suite runs. Do not edit `docs/superpowers/**`.
- Commits small, `shot_design:`-prefixed, trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. Report `.superpowers/sdd/task-LLM1-report.md`: prompt v5 text, gate rules, column/manifest contract, exact operator runbook (sbatch → wait for endpoint.json → dry run 5 → full run → restart serve), verification output, anything not done.
