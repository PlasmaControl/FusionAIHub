# Appendix C — Codex gpt-6-astra (max) critique: findings retained for execution
1. Old shotsearch manifest counts the wrong axis → never reuse; fresh census. 2. Existing AE model AUROC 0.62 vs humans → state label quality; evaluation before new claims. 3. IGNITE decode → spectrograms, not waveforms. 4. Actuator contract provenance: obtain the production cache builder from the prod_v2 owner when possible (G-ENC substitutes measured verification). 5. Prior TokEye job 0.6–1% GPU util at 97% GPU mem → pilot-sized requests; memory occupancy is not work. 6. MCP is prior art (Smith §7.2) → differentiator is the phenomenon evidence DB. 7. Forecast ≠ observed event. 8. Circularity: keep actuator context out of diagnostic classifiers used for circumstances; definitional associations (QH ≡ EHO ∧ ELM-free) must be labelled as such. 9. Annotation: blind review, hard negatives, random windows are "unreviewed" not negatives, one annotator → GroupKFold by shot and an independent audit set later. 10. Storage ~30 TB naive → compact masks. 11. Rollout latency → async. 12. Text: 2,302/22,950 shots fall back to session text → represent as run-scope, not shot-scope; reuse shotrec negation/mitigation parsing. 13. `validate.py` reports every row held-out when training shots unknown → represent as "membership unknown". 14. Circumstances below 30 independent case shots → descriptive only; report exposure (event rate per valid plasma-second). 15. Rubric caps (fabricated provenance / synthetic-as-observed / invalid→negative / leakage → ≤4; unmet jobstats or no independent eval → ≤7).
