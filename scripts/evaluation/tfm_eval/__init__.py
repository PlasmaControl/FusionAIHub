"""Evaluation-suite library for the e2e tokamak foundation model.

Import as a plain package from ``scripts/evaluation`` (drivers bootstrap
``sys.path`` for both this directory and the repo ``src/``).
"""
