No formatted labels exist yet. The planned CSV columns are
`shot,category,t_start,t_end,confidence`, with milliseconds declared in
`../../events.yaml`. See the [event README](../README.md) for category definitions
and the work needed before generating labels.
