# Task R1 — rename `ideate` → `shot_design` and `labelmaker` → `labeler`

Worktree `/scratch/gpfs/nc1514/FusionAIHub-R1`, branch `recommender-R1` from `recommender` @ 09ee14d. User directive 2026-09-15 with these scope decisions (binding):

1. **Scope = code + env vars; on-disk data paths, file formats and manifest/data keys are KEPT.** Rename the Python packages, their test/script/config/doc trees, CLI module names (`python -m shot_design …`, `python -m labeler.run …`, `python -m labeler.jobstats …`), the MCP server name (`MCPServer("shot_design")`, `.mcp.json` key `shot_design`, command `python -m shot_design.mcp`), pixi tasks (`shot_design`, `shot_design-mcp`, `shot_design-test`, plus any `labelmaker*` task), hatch `packages` entries, and the environment variables: `IDEATE_DATA_ROOT` → `SHOT_DESIGN_DATA_ROOT`, `IDEATE_CORPUS` → `SHOT_DESIGN_CORPUS`, every other `IDEATE_*` → `SHOT_DESIGN_*`, `LABELMAKER_ROOT` → `LABELER_ROOT`, every other `LABELMAKER_*` → `LABELER_*`. **Each config reader accepts the new name first and falls back to the old name** (one shared helper per package, a one-line warning only when the OLD name is the one that resolved), with tests for both directions. Do NOT change: the data roots `/scratch/gpfs/EKOLEMEN/nc1514/{labelmaker,ideate}` and `$…/envs/phase3`, `models/tokeye/…` paths (they are the default VALUES of the renamed variables), on-disk file names (`*_events.parquet`, `*_sources.parquet`, `*_masks.npz`, `*_labels.h5`, `*_features.h5`, `events_index.parquet`, `db/*.parquet`, `manifest.json`), manifest/data keys such as `labelmaker_git_sha`, HDF5 attrs, run-record JSON keys, jobstats fixture texts under `tests/…/data/` (they are captured real output: rename their directory with the tree, leave their contents byte-identical), and anything under `data/events/**` (the user's).
2. **pixi environments** are renamed in `pyproject.toml` (`labelmaker` → `labeler`, `ideate` → `shot_design`, `ideate-cpu` → `shot_design-cpu`; features likewise) and the `environments:` keys in `pixi.lock` are renamed by TEXT EDIT ONLY (same package lists; `--frozen` reads it as-is). **Never run `pixi install`, `pixi lock`, `pixi update`, or a bare `pixi run`.** To run the suites under the new names WITHOUT creating any environment (file quota 1.7M/2.0M): create `.pixi/envs/` in THIS worktree containing only symlinks `labeler -> /scratch/gpfs/nc1514/FusionAIHub/.pixi/envs/labelmaker`, `shot_design-cpu -> /scratch/gpfs/nc1514/FusionAIHub/.pixi/envs/ideate-cpu`, `shot_design -> /scratch/gpfs/nc1514/FusionAIHub/.pixi/envs/ideate`, then use `pixi run --frozen --no-install --manifest-path /scratch/gpfs/nc1514/FusionAIHub-R1/pyproject.toml -e labeler|shot_design-cpu …`. First probe with `python -c "import sys; print(sys.executable)"` and check `du -s .pixi` stays at symlinks only; if pixi refuses the symlinked environments (lock-hash / environment-name check) even with `--no-install`, STOP that route: keep the pixi env names `labelmaker`/`ideate`/`ideate-cpu` (the user's stated fallback), revert the lock/env-name edits, and record it in the report. Whatever happens, the worktree `.pixi` must contain nothing but symlinks (verify with `find .pixi -type f | wc -l` = 0) and must be removed before you finish.
3. **Mechanics.** `git mv` for every directory/file so history follows: `src/ideate → src/shot_design`, `src/labelmaker → src/labeler`, `tests/ideate → tests/shot_design`, `tests/labelmaker → tests/labeler`, `scripts/ideate → scripts/shot_design`, `scripts/labelmaker → scripts/labeler`, `configs/ideate → configs/shot_design`, `docs/IDEATE.md → docs/SHOT_DESIGN.md`, `docs/LABELMAKER.md → docs/LABELER.md`, `outputs/labelmaker → outputs/labeler` (update module references in its scripts only; recorded results untouched). Then rewrite references: imports (`import ideate`, `from ideate…`, `labelmaker.`), string literals (`"ideate"`, `"labelmaker"` as module/env/server/task names, `python -m …`, `-e ideate-cpu`, `-e labelmaker`, `PYTHONPATH` notes), docstrings, comments, READMEs and HF model cards under `src/labeler/models/*/README.md`, `docs/*.md`, `scripts/**` (`.sbatch`, `.sh`, `.py`), `configs/**`, `pyproject.toml`, `.mcp.json`, test path constants (e.g. `parents[2] / "scripts" / "labelmaker"`), `tests/**/conftest.py` monkeypatched env names, `src/shot_design/mcp/server.py` INSTRUCTIONS/name, `src/shot_design/ui/**` static strings that name the CLI (`ideate serve` → `shot_design serve`). Word-boundary aware: do not touch unrelated words, URLs, or the data-path components named in (1). Human-facing product names in prose ("IDEATE", "labelmaker v2") become "shot_design" / "labeler" consistently — the user's names win.
4. **Do not touch**: `docs/superpowers/**` (plans/specs/ledger are history), `.superpowers/sdd/**` other than your own brief/report (historical reports), `data/**`, `src/tokamak_foundation_model/**`, `src/faith/**`, `tests/ignite`, `tests/e2e`, `CLAUDE.md`, `AGENTS.md`, `pixi.lock` beyond the environment-key rename, anything outside this worktree. The three sbatch/companion scripts' `REPO` defaults should point at `/scratch/gpfs/nc1514/FusionAIHub` (main checkout) instead of removed worktrees; `--manifest-path` stays the main checkout's `pyproject.toml`.
5. **Verification (all from this worktree):** `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=$PWD/src HF_HUB_OFFLINE=1 OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 pixi run --frozen --no-install --manifest-path <this worktree>/pyproject.toml -e labeler python -m pytest tests/labeler -q -W error -p no:cacheprovider` and `… -e shot_design-cpu python -m pytest tests/shot_design -q -W error -p no:cacheprovider` (or the old env names + main manifest if the fallback applies); ruff `/scratch/gpfs/nc1514/FusionAIHub/.pixi/envs/labelmaker/bin/ruff check --no-cache src/labeler src/shot_design scripts/labeler tests/labeler tests/shot_design`; `grep -rnE "\b(ideate|labelmaker)\b|IDEATE_|LABELMAKER_" src tests scripts configs docs/*.md pyproject.toml .mcp.json` must return ONLY the intentional fallback names and data-path values (list every remaining hit in the report with its reason); `python -m shot_design --help` and `python -m labeler.run --help` (via the pixi form) exit 0; `git status` clean, no `.pixi` left in the worktree. Expected counts before the rename: labeler suite 1,655 passed / 3 skipped, shot_design suite 1,269 passed — the same afterwards (renames add only the env-fallback tests). Do NOT commit while a suite runs.
6. **Commits**: one for the `git mv` moves (pure renames, so `git log --follow` works), one per reference-rewrite group (imports/strings; pyproject + lock + `.mcp.json`; env-var fallback helpers + tests; docs/cards; scripts), then the report. Trailer on every commit: `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. Subjects prefixed `rename:`.
7. **Report** `.superpowers/sdd/task-R1-rename-report.md`: what moved, the reference groups, the pixi outcome (symlink probe result verbatim), suite counts + ruff, the residual-hit list, and a **post-merge checklist for the controller**: the exact `mv` commands for the main checkout's `.pixi/envs/<old> <new>` (and the symlink alternative), which long-lived processes to restart (`shot_design serve`, the MCP server in `.mcp.json`), memory/notes that name the old env vars, and the two production data roots that intentionally keep their old names.

Binding rules from the ledger: nothing new under `/scratch/gpfs/nc1514` except this worktree's source (scratch under `/tmp/r1rename`); no production data reads or writes are needed for this task and none are permitted; never edit `docs/superpowers/plans/**`.
