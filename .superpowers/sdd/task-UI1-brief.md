# Task UI1 — Shot Designer GUI: theme, layout, formatting, structured shot page, summary slot

Worktree `/scratch/gpfs/nc1514/FusionAIHub-UI1`, branch `recommender-UI1` (from `recommender` @ 3a08ba0). Executor: Codex gpt-6-astra, write mode. Controller verifies in a clean worktree, then merges.

## What the user asked for (verbatim intent, 2026-09-15)

Screenshots of the current GUI (`python -m shot_design serve`) and of the group's slide template (dark green banner, white body) drove these requests:

1. **Title** "Shot Designer" (header). Do not show the build sha. Keep "504 shots · 185786–204925".
2. **Theme** like the group's slides: white page, dark teal-green banner header with white text, the same green for links/buttons/active tab; light neutral panels. Approximate banner colour `#2f6f66` (muted teal-green); pick a coherent palette from it (hover, focus, borders). Good contrast throughout.
3. **Shot number must be fully shown** — the Shot column currently wraps "199607" into three lines. `white-space: nowrap`, a min-width, tabular/monospace digits. Same for every place a shot number renders.
4. **Replace the Quote column** in results with a **Summary** column. **Amended by the user:** use the existing `blurb` and `blurb_source` (`"llm" | "template"`) columns in `shots.parquet`; do NOT add `summaries.parquet`. Render `blurb` as the Summary text in results and the shot page's Summary block. When `blurb_source != "llm"`, add a small muted `auto` tag next to the text; the template blurb is the factual header + outcome line. Ensure `SearchHit` and `/api/search` rows carry `blurb` and `blurb_source`, adding fields if needed. The follow-up LLM task writes its three short sentences (goal; success/failure; interesting finding) into this same column through the existing blurb pipeline. Missing text displays "—" in results and hides the shot Summary block. The attributed logbook quote stays on the shot page and leaves the results table. Everything else in this brief stands.
5. **Long text → clamp + expand**: any table cell / list item longer than ~140 chars or 2 lines (caveats, summary, run/MP titles, notes) shows the clamp with "…" and a click toggle ("more" / "less"). Results, Locate hits, and the shot page.
6. **Numbers**: one shared front-end formatter used EVERYWHERE a measured number renders: 4 significant digits, trailing zeros stripped; scientific notation when |x| ≥ 1e5 or 0 < |x| < 1e-3 (`7.13e14`, `8.924e5`, `1.2e-4`); otherwise plain (`2.82`, `0.9457`, `892`). Times in seconds with 3 decimals (`1.321–1.792 s`), confidences 3 decimals. Identifiers are never reformatted: shot numbers, years, dates, counts (`+241 more`), run ids, MP numbers.
7. **Units** on per-segment scalars: `configs/shot_design/signals.yaml` has `units:` per signal (A, T, m, V, G, J, m^3, m/cm3, ph/cm2/sr/s). Expose a `units` map from the API (per scalar column, derived stat → signal → units; check how `slope` is computed and label it `<unit>/s` only if that is what it is, else leave slope unitless) and render `ip_mean  8.924e5 A`.
8. **Tall sections collapsed by default**: per-segment scalar cards (and any other section that runs longer than ~260 px: event lanes lists, coverage) get a fixed max-height with a fade and a "Show all / Show less" toggle.
9. **Event timeline**: keep the lane graphs; REMOVE the per-event bullet lists under each lane (they fill the page). Keep the information reachable via a hover `title` (time span, phenomenon, evidence kind, confidence) on each bar.
10. **Shot page**: (a) first a **Summary** block (item 4's text; hidden when `None`); (b) then the shot text formatted into sections instead of one dense paragraph. Do this deterministically and SERVER-SIDE: `describe()` keeps returning its string (MCP contract), and a new structured form (e.g. `describe_parts(rec, segment, db)` → `{header, scalars: [{name, value, units}], outcome, operator_quote: {text, author, time}, phenomena: [{id, title, n_observed, first_intervals, n_forecast, coverage_note}], caveats}`) is what `/api/shot` returns alongside the string; the UI renders header · key/value grid with units · Outcome · attributed quote · a phenomena table (one row per phenomenon: name, observed count, first three intervals, forecast count, coverage). No client-side parsing of the paragraph.
11. **Wording**: remove explanatory, hedging, "AI-sounding" prose from UI captions, headings, notes and caveats while KEEPING the information (coverage bounds, forecast ≠ observation, run-scope text, unprocessed ≠ quiet). Examples of the target register:
    - "text evidence is run scope: a session-wide sentence, not this shot's logbook" → "run-level text, not shot-specific"
    - "44 forecast row(s) are in `forecasts`, not in `events`: a forecast is a model's claim about what was about to happen, not an observation of what did" → "44 forecasts (model estimates) shown separately"
    - "1 source(s) ran over shot 199607 and recorded NO coverage -- actuator/ech_power_total: ran; coverage unknown -- says nothing about this window, neither that it was looked at nor that it was not" → "actuator/ech_power_total: coverage unknown"
    - "forecasts — a model's risk estimate, not an observation" (heading) → "Forecasts (model estimates)"
    - "Curated database intervals — entries in a list, not measurements" → "Database intervals (curated lists)"
    - "the Transient activity detectors covered only [0.9873090865135192, 3.797149896621704] s of the flat_top window; absence outside that is unmeasured" → "covered 0.987–3.797 s of flat_top only"
    Scope: UI-only strings (`ui/static/*`, `ui/app.py`) and any `describe`/`schema` caveat strings the UI shows. Do NOT change the MCP tool descriptions or `get_events` `status` semantics in `mcp/tools.py`; if a string is shared between MCP and UI, shorten it at the source only if every existing test and the MCP docstring meaning still hold, otherwise map it in the UI layer. Never render an unavailable value as 0; forecasts never appear in event lanes.
12. Header shows no build sha (keep it in `/api/meta` for tooling).

## Where things are
`src/shot_design/ui/static/{index.html,app.js,style.css}` (vanilla JS module; `el()`, `display()`, `resultsTable()`, `timeline()`, `renderEvents()`, `renderShot()`, `renderHit()`), `src/shot_design/ui/app.py` (FastAPI: `/api/meta`, `/api/search`, `/api/shot/{shot}`, `/api/shot/{shot}/events`, `/api/phenomena`, `/api/locate`), `src/shot_design/retrieval/describe.py` (`describe`, `_phenomenon_line(s)`, `best_quote`, `polish`), `src/shot_design/schema.py` (`ShotRecord`, `SearchHit.quote/quote_role/caveats`), `src/shot_design/shotdb/store.py` (`ShotDB.load`), `configs/shot_design/signals.yaml`, tests `tests/shot_design/test_ui.py`, `test_ui_intervals.py`, `test_describe.py`, `test_phenomenon_describe.py`. Production DB (READ-ONLY for you): `/scratch/gpfs/EKOLEMEN/nc1514/ideate/db` (504 shots); a serve for manual checks: `pixi run --frozen --no-install --manifest-path /scratch/gpfs/nc1514/FusionAIHub/pyproject.toml -e ideate-cpu python -m shot_design serve --port 87xx` from this worktree with `PYTHONPATH=$PWD/src` (the activation env already points `SHOT_DESIGN_DATA_ROOT` at production; you only read).

## Rules (binding)
- Every `pixi run` is `pixi run --frozen --no-install --manifest-path /scratch/gpfs/nc1514/FusionAIHub/pyproject.toml -e <env>`; never bare `pixi run`, never `pixi install/lock/update`; no `.pixi` in this worktree at the end.
- Nothing new under `/scratch/gpfs/nc1514` except this worktree's source. No production writes anywhere (the DB, `$LABELER_ROOT`, `$SHOT_DESIGN_DATA_ROOT`). Tests write only to `tmp_path`.
- TDD: tests first for the API-side additions (units map, `describe_parts`, `shots.parquet` blurb/source loading incl. absent → None, `SearchHit.blurb` and `SearchHit.blurb_source`), and for every shortened shared string. JS has no test runner: `node --check` each module and exercise the served page's API with curl/httpx; put the number-formatter rules and 8 worked examples in a comment block next to it.
- Verify: `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=$PWD/src HF_HUB_OFFLINE=1 OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 pixi run … -e ideate-cpu python -m pytest tests/shot_design -q -W error -p no:cacheprovider -rs` (expect ≥ 1,375 passed, 0 skipped) AND `… -e labelmaker python -m pytest tests/labeler -q -W error -p no:cacheprovider -rs` (expect 1,775 passed, 3 skipped), ruff `.pixi/envs/labelmaker/bin/ruff check --no-cache src/labeler src/shot_design scripts/labeler tests/labeler tests/shot_design` from the main checkout's `.pixi`. Never commit while a suite runs.
- Commits: small, `shot_design:`-prefixed subjects, trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. Do not edit `docs/superpowers/**`. Update `docs/SHOT_DESIGN.md` (GUI section: title, theme, summary slot, `describe_parts`).
- Report to `.superpowers/sdd/task-UI1-report.md`: what changed per item 1–12, the API shape of `describe_parts` and `units`, the blurb/source column contract, every wording change (before → after), verification output, and anything you could not do.
