# Task L14-perf-fix — apply review findings F1–F13, run the second right-sized pilot

Worktree `/scratch/gpfs/nc1514/FusionAIHub-L14perf`, branch `recommender-L14perf` @ fa63f7a. Review: `.superpowers/sdd/review-L14perf.md` (verdict FIX-THEN-MERGE; read it in full — every finding has file:line and a concrete fix). All rules of `task-L14perf-brief.md` (incl. the RESUME addendum) stay binding: mandated `pixi run --frozen --no-install --manifest-path /scratch/gpfs/nc1514/FusionAIHub/pyproject.toml -e <env>` form for every pixi invocation, no `pixi install`, no `.pixi` in the worktree, nothing new under `/scratch/gpfs/nc1514` except this worktree, data writes only under `/scratch/gpfs/EKOLEMEN/nc1514/labelmaker/runs/l14perf/` plus the prescribed `runs/slurm/` gate captures, no production masks/events/labels/features writes, never edit `docs/superpowers/plans/**`, never commit while a suite runs, no threshold/tolerance changes, TDD (failing test first) for every code fix. Commit per finding group with the trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`; commit the review file first.

## Blocking — code
- **F1** `src/labelmaker/events/masks.py:628-632` (reference/CUDA path): on `copied is None`, `torch.cuda.empty_cache()` and retry ONCE inside `try/except torch.cuda.OutOfMemoryError`; on the second failure do NOT raise — set `state.error` on every state in `spans` and `continue` so `collect()` yields those blocks as errors (per-block isolation, as the module docstring promises). Preferred stronger variant: copy each forward group separately (`_copy_batch(host[begin:begin+count], …)`) and feed `consume` per group, so the retry is genuinely smaller. Regression test with `preserve_batches=True` and a `_copy_batch` stub that fails the first N calls; assert the other shots in the chunk still complete.
- **Default `preserve_batches=True` on every device** (reviewer's recommendation, F13/B): the measured win came from pooled transfers + device compaction (D2H 2.454 GB → 0.108 GB), not from cross-block forwards, and CPU cross-block exactness is an empirical single-threaded result. Keep CPU cross-block forward pooling behind an explicit opt-in flag. Pin the default with a CPU-runnable test (**F8**: factor `_preserves_reference_batches(device)` and assert it for both `cpu` and `cuda` devices without needing a card).
- **F3** `masks.py:651-680` + `driver.py:838-840, 981`: attribute a `StageTimeout` per span — only states whose own `remaining(state) <= 0` get the error; the rest of the batch completes. Regression: two shots, `tile_batch=4`, a model that sleeps past shot i's deadline; shot i+1's blocks succeed.
- **F4** `masks.py:666-667`: reset `size` to `batch` at each block boundary in the non-reference path (as `_ReferenceBatches` does).
- **F7** `driver.py:915-925`: on tail-pool restart resubmit in place (keep the entry's position in `pending`), so rows stay in plan/shot order with >1 tail worker. Regression with 2 tail workers and a forced `BrokenExecutor`.
- **F6** `tests/labelmaker/test_tokeye_masks.py`: parametrise the prep-pool-death / tail-pool-restart / unpicklable-payload / task-timeout / worker-crash isolation tests over `pooled` (or add pooled equivalents), so the DEFAULT scheduler is what they cover; add a test that `tail_workers > 1` with `index=True` is refused (as an argparse/`SystemExit` error, not a bare traceback).
- **F11** `driver.py:1142-1145`: replace `dict(locals())` with an explicit options dict or a small frozen dataclass.
- **F12** `_ReferenceBatches.consume`: real exceptions instead of bare `assert`s (they vanish under `-O`).
- Also add the one-line assert/comment at `pipeline.py:822-823` that `transients.ACTIVITY_THR == masks.PROB_THRESHOLD` (the compact path relies on it).

## Blocking — sizing (F2) and the second pilot
The 10 CPU / 6 prep / prefetch 6 / 3 tail / 31 G / 4 min proposal is below your own measurements (tail 3.04 cores, prep 7.2 cores, `--time` == `--timeout 240`). Replace it. Run ONE more ≤20-shot A100 pilot (exempt but reported) at exactly the reviewer's table — **`--cpus-per-task=12` (PREP_WORKERS=7, TAIL_WORKERS=4, +1 parent), PREFETCH=8, `--mem=32G`, `--time=00:12:00`, 1 A100, TILE_BATCH=96, AMP, `--no-index`, same `SHOT_FILE=…/runs/slurm/l12/pilot20.txt`**, `ROOT=/scratch/gpfs/EKOLEMEN/nc1514/labelmaker/runs/l14perf/pilot2-l14perf`, `REPO=/scratch/gpfs/nc1514/FusionAIHub-L14perf` at the fixed HEAD (all code fixes above committed first). Gate it with `python -m labelmaker.jobstats --job-id <id> --pilot --preserve-dir …/runs/slurm --out …/runs/slurm/jobstats.json`, report all four percentages + sacct MaxRSS + TotalCPU + tiles/s as measured (a miss is diagnosed, never gamed; no re-sizing to pass). Verify 20/20 shots, block/event counts vs run rows, and that this pilot's NPZ/events/sources hashes equal pilot 2931999_0's for every shot (same code semantics; if they differ, that is a finding, not a tolerance). Then derive the production request from THIS pilot's measurements (× 1.3 for memory; CPUs = `PREP + TAIL + 1`; time = startup + one `--timeout` + 60 × per-shot × 1.3), state it as report-only. Production is NOT to be submitted.

## Non-blocking — docs/report
- **F5** state the identity condition ("byte-identical when no OOM halving occurs or when it occurs identically") in the report and `docs/LABELMAKER.md`.
- **F9** `scripts/labelmaker/tokeye_masks.sbatch`: rewrite the header's measured basis from the second pilot; set the SBATCH defaults to the derived production request; `--output` must follow `ROOT` when `ROOT` is overridden; `tests/labelmaker/test_tokeye_sbatch.py:28` asserts `cpus == prep + tail + 1` parsing `TAIL_WORKERS`.
- **F10** restore the deleted rationale in `driver.py`'s module docstring (events_index lost-update analysis → `--no-index`; `text/logs_subset.jsonl` pre-pass and why `readonly` changes what is written; SIGALRM semantics; memory arithmetic per `(512,T)` array; the CPU-per-channel note) and the runnable pilot workflow in `docs/LABELMAKER.md` (text pre-pass `tokeye_text_subset.sh`, array submission, `--dependency=afterok` rebuild+gate via `tokeye_masks_afterok.sbatch`, compute nodes lack `/usr/local/bin/jobstats` so the pre-pass stages the client into `runs/slurm/jobstats-client/`) alongside the new paragraphs.
- **F13** reconcile the two ideate timings (132.69 s vs 126.47 s — say they are different runs); note that float32 vs float64 overlap accumulation is indistinguishable at TILE 512/STRIDE 448 (≤2 tiles per column, exact halving) so the float64 choice is belt-and-braces, not test-proven.

## Finish
Full suites from the worktree with the mandated form (labelmaker + ideate, `-q -W error -p no:cacheprovider`), ruff over `src/labelmaker src/ideate scripts/labelmaker tests/labelmaker tests/ideate`, no commit while a suite runs. Append a "Fix loop" section to `.superpowers/sdd/task-L14perf-report.md`: per finding what changed + file:line, the second pilot's full measurements and gate output, the derived production request, suite counts, ruff, anything skipped with the reason. Final message: that section + `git log --oneline fa63f7a..HEAD`.
