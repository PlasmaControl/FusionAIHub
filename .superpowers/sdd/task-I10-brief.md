# Task I10 — MCP tool `phenomenon_locate` (ideate)

Worktree `/scratch/gpfs/nc1514/FusionAIHub-I10`, branch `recommender-I10` cut from `recommender` @ a3db1e7. Plan §7/§B7 and `docs/superpowers/specs/2026-09-07-recommender-ideate.md:45`: `phenomenon_locate(phenomenon, n, segment, constraints) -> list[PhenomenonHit]`. The CLI already does this (`ideate phenomenon TEXT`, `src/ideate/cli.py::cmd_phenomenon`) over `retrieval/phenomena.locate(...)`; the MCP server exposes only `search_shots`, `describe_shot`, `get_events` (`src/ideate/mcp/tools.py::TOOLS`, `mcp/server.py::build_server`). Make the fourth tool, to the same contract as the other three.

## Deliverable
`src/ideate/mcp/tools.py::phenomenon_locate(phenomenon: str, n: int = 20, segment: str = "flat_top", constraints: dict | None = None, min_confidence: float = 0.0, avoid: list[str] | None = None) -> dict`, decorated `@never_raises`, appended to `TOOLS`. Behaviour, mirroring `cmd_phenomenon`:
- `resolve(phenomenon)` — free text or an id. Nothing resolves → `{"error": "no phenomenon resolved ...", "caveats": [...]}` that lists the registry's `id (title)` pairs (the CLI's exit-2 case; an empty hit list would read as "no shot has one", the opposite answer).
- Incomplete/missing database → the same `_incomplete_db()` / `_error` path the other tools use.
- `locate(top_id, db, n, segment=..., constraints=..., min_confidence=..., avoid=avoid or (), notes=notes)`; `constraints` parsed with the existing `_range` exactly as `search_shots` does (unknown column → error dict, not an exception); `segment` validated with `_segment`.
- Payload: `{"phenomenon": top_id, "title": ..., "resolved": [{"id", "title", "weight"}...], "n": len(hits), "hits": [PhenomenonHit.model_dump(mode="json") ...], "notes": [...], "caveats": [...]}`. `notes` are `locate`'s sentences about shots NOT in the result (what `avoid` dropped, on which evidence) — keep them a separate list and say in `caveats` that they describe excluded shots. Top-level `caveats` must also carry: `ALL_FORECASTS` when the database holds only forecast rows (as the CLI prints), the segment caveat if any, and the instructions' standing rule that a text-only or curated-list hit is not an observation (reuse the vocabulary in `phenomena.py`; do not invent new sentences where one exists). Every hit already carries its own `caveats`; do not strip them.
- `mcp/server.py::INSTRUCTIONS`: add the tool in one short paragraph in the existing voice (what a hit's evidence class means; ordering observed > label > forecast > database > text; `notes` are about excluded shots). Do not restate the whole `get_events` text.
- `docs/IDEATE.md` "### The tools" table: one row for `phenomenon_locate` in the same format as the others; the "The MCP server" prose should mention it exists where it lists the three.
- Tests in `tests/ideate/test_mcp.py` (follow its fixtures and style; the `ideate_db` fixture is the model): resolves an alias and returns hits ordered by evidence class then score; no-resolution → error dict with titles, never an empty list; `avoid` drops an observed shot and the dropped count appears in `notes`; unknown constraint column → error dict; incomplete db → the shared error/caveats; `caveats` non-empty on every payload; the tool is in `TOOLS` and `build_server()` lists it (if a test pins the tool names, extend it). TDD: write the failing tests first, commit them, then the implementation.
- Read-only production smoke, and paste the output in the report: `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=$PWD/src HF_HUB_OFFLINE=1 pixi run --frozen --no-install --manifest-path /scratch/gpfs/nc1514/FusionAIHub/pyproject.toml -e ideate-cpu python -c 'from ideate.mcp import tools; import json; r = tools.phenomenon_locate("edge harmonic oscillation", n=5); print(json.dumps({k: (v if k != "hits" else [{kk: h[kk] for kk in ("shot", "score", "caveats")} for h in v]) for k, v in r.items()}, indent=1, default=str))'` — this READS the production DB through the activation env; it writes nothing. Also time it three times (warm) and report the ms against the 300 ms budget in `src/ideate/eval/latency.py`'s table; do NOT tune anything to meet it — report.

## Binding rules
- Work ONLY in this worktree on `recommender-I10`. Do not touch `configs/ideate/phenomena.yaml`, `tests/ideate/test_phenomena.py`, `data/events/**`, or anything under `src/labelmaker/**`, `scripts/labelmaker/**`, `tests/labelmaker/**` (two other fronts own those right now). Never edit `docs/superpowers/plans/**`. Never check out or commit to `recommender`.
- **Every `pixi run` MUST be exactly** `pixi run --frozen --no-install --manifest-path /scratch/gpfs/nc1514/FusionAIHub/pyproject.toml -e ideate-cpu ...` from this worktree with `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=$PWD/src HF_HUB_OFFLINE=1`. A bare `pixi run` here would solve and install a 5 GB / 124k-file `.pixi` under `/scratch/gpfs/nc1514` (file quota 1.7M/2.0M). Never `pixi install`. Create nothing under `/scratch/gpfs/nc1514` except files of this worktree; scratch under `/tmp/i10`. Never run an ideate WRITE command (`build`, `add`, `labels join`, `encode`) at all in this task.
- Suite: `python -m pytest tests/ideate -q -W error -p no:cacheprovider` (ideate-cpu env) must exit 0; `ruff check src/labelmaker src/ideate scripts/labelmaker tests/labelmaker tests/ideate` clean. XRootD's atexit FutureWarning after the summary is not a failure — check the exit code. Do not commit while the suite runs.
- Commits: per deliverable, evidence-stating bodies, subject prefix `ideate:`, final line `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
- Report `.superpowers/sdd/task-I10-report.md` (tracked, in this worktree): what changed and why, the test list, the production smoke output, the three latency timings, deviations, the suite summary with exit code, `git log --oneline recommender..recommender-I10`.
