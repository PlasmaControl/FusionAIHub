# Thin browser UI: only server is read; there are no plot, model or retrieval settings here.
version: 1
server:
  host: 127.0.0.1  # local only; remote access uses ssh -L 8765:localhost:8765 stellar
  port: 8765
