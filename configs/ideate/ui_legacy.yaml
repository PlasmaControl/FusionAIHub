# Compatibility for retrieval.actuation and retrieval.scenarios, which still ask
# config.load_yaml("ui.yaml") for these defaults. The thin browser UI reads none of them.
landing:
  prompt: "What do you want to test?"
  n_scenarios: 6
  n_hits: 50
actuation:
  rdp_tol_frac: 0.03
  max_vertices: 12
  grid_ms: 50
