# FusionAIHub


This will be the readme of the FusionAIHub.

When you are in the root directory, you can run the following command to activate the virtual environmnent:
```
module load anaconda3/2024.10
python -m venv .venv
source .venv/bin/activate
pip install --upgrade pip
pip install uv
uv sync
```